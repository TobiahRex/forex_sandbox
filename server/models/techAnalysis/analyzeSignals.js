const moment = require('moment');

function analyzeSignals(signals) {
  const buySignals = generateBuySignals(signals);
}

function generateBuySignals(signals) {
  // console.log('signals: ', signals);
}

module.exports = analyzeSignals;
